# This file builds the Injector.exe application.

# Define the project name (optional, but good practice)
project(Injector)

# Create the executable from its main source file.
add_executable(Injector src/main.cpp)